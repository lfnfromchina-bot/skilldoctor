---
name: {{NAME}}
description: >-
  {{DESCRIPTION}}
  使用场景：当用户说 {{TRIGGER_LIST}} 等类似表达时加载此技能。
---

# {{NAME}}

## 何时使用
{{TRIGGERS}}

## 何时不要使用
- （列出容易误触的相邻需求）

## 执行方式

运行 `scripts/run.py` 完成核心处理，不要手写等效逻辑：

```bash
python scripts/run.py --input <用户提供的输入>
```

脚本输出即为结果草稿，按下面的质量标准检查后再交付。

## 质量检查清单
- [ ] 脚本退出码为 0
- [ ] 输出满足用户的核心诉求

## 防护栏
- 不要编造用户未提供的事实或数据；信息不足时先向用户确认。
- 脚本报错时展示原始错误信息，不要假装执行成功。
